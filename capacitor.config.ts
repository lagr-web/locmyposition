import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'edu_logmypostion',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
